database:
  driver: postgres
  host: localhost
  port: 5432
  name: fhelper
  user: root
  password: admin
  sslmode: disable
#  source: postgresql://LOGIN:PASSWORD@host:port/name?sslmode=sslmode
grpc:
  host: localhost
  port: 50051
  enable-tls: false
  timeout: 10h
token_ttl: 1h